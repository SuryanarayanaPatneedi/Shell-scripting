- name: demo on vars
  hosts: all
  vars:
    URL: play.example.com
  tasks:
    - name : print variable
      debug:
        msg: URL = {{URL}}

    - name: print var on task level
       vars:
         URL: task.example.com
         debug:
           msg: URL={{URL}}